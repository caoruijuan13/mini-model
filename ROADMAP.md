# mini-model 项目路线图

本文档只负责规划学习顺序、后续阶段、阶段边界和验收标准。项目定位、已完成内容、Token 概念和运行入口见 [总项目说明](README.md)。

## 使用规则

- `[x]` 表示该项已完成并已有当前代码、测试或报告证据；
- `[ ]` 表示该项尚未完成；
- 实现过程中只更新对应复选框，不重新排列既定阶段；
- 一个阶段的实现项和验收项全部勾选后，才把“阶段完成”勾选，并更新阶段总览；
- 如果路线发生实质变化，保留原阶段及记录，另行补充调整原因，不覆盖历史结果。

## 路线目标

项目沿着一条连续主线，从可以手工验证的统计模型逐步进入神经语言模型和推理工程：

```text
数值特征
  → 逻辑回归
  → 字符序列与条件概率
  → 字符 bigram
  → 字符 trigram 与 backoff
  → 显式 Tokenizer 与 token ID
  → token embedding 与 MLP
  → mini-batch、Adam 和 checkpoint
  → causal self-attention 与 Transformer
  → subword tokenizer
  → 模型评估、推理和服务化
```

这条主线依次回答：

1. 模型计算什么：概率、损失、梯度、注意力和生成；
2. 文本如何进入模型：文本、token、token ID、embedding；
3. 模型如何完成训练：样本、反向传播、优化器和 checkpoint；
4. 模型如何可靠运行：加载、推理、流式输出和性能评估。

## 阶段总览

| 阶段 | 完成 | 阶段目标 | 进入下一阶段的条件 |
| --- | --- | --- | --- |
| 01 逻辑回归 | [x] | 建立监督学习、loss 和梯度下降闭环 | 测试与运行报告已完成 |
| 02 字符 bigram | [x] | 建立字符级下一 token 预测闭环 | 测试、持久化和语料对照已完成 |
| 03 字符 trigram | [x] | 理解更长上下文、稀疏性和 backoff | 测试、验证集调参与基线对照已完成 |
| 04 显式字符 Tokenizer | [x] | 建立稳定的文本与 token ID 边界 | encode/decode、未知字符和持久化验收通过 |
| 05 Token MLP | [ ] | 从计数表进入 embedding 和神经网络 | 反向传播正确并完成 trigram 对照 |
| 06 训练工程 | [ ] | 建立可恢复、可复现的训练流程 | mini-batch、Adam、checkpoint 验收通过 |
| 07 字符级 Transformer | [ ] | 理解 causal self-attention 和自回归生成 | mask、张量形状、训练和生成测试通过 |
| 08 Subword Tokenizer | [ ] | 理解现代大模型的子词 token | 完成字符与 subword 的可重复对照 |
| 09 推理与服务化 | [ ] | 分离训练和运行时，验证性能边界 | 批量/流式推理及性能报告完成 |

“已完成”只表示当前教学目标及其证据闭环完成，不表示模型达到生产质量。历史阶段保留独立实现和报告，后续阶段不覆盖其结果。

## 01：逻辑回归

阶段状态：

- [x] 阶段完成

### 实现清单

- [x] 生成固定随机种子的二分类合成数据；
- [x] 分离训练集、验证集和测试集；
- [x] 实现线性函数与 sigmoid；
- [x] 实现二元交叉熵；
- [x] 手写权重和偏置梯度；
- [x] 使用全批量梯度下降更新参数；
- [x] 使用验证集执行 early stopping；
- [x] 恢复验证集表现最好的参数；
- [x] 在独立测试集上报告 loss 和 accuracy；
- [x] 编写设计、学习、测试和运行文档。

### 验收清单

- [x] 数据形状、切分比例、标签范围和固定 seed 测试通过；
- [x] sigmoid 数值范围和 `sigmoid(0)=0.5` 测试通过；
- [x] 可手算的二元交叉熵基准测试通过；
- [x] 单步参数更新方向与 loss 下降测试通过；
- [x] 完整训练和测试集 accuracy 基线测试通过；
- [x] 最佳验证集参数恢复测试通过；
- [x] 当前 6 个自动化测试通过；
- [x] 当前运行报告记录测试集 accuracy `91.8333%`；
- [x] 文档明确合成数据、单随机种子和指标范围等限制。

## 02：字符 bigram

阶段状态：

- [x] 阶段完成

### 实现清单

- [x] 使用一个字符预测下一个字符；
- [x] 从文本自动构造相邻字符自监督样本；
- [x] 建立字符词表和字符到 ID 的映射；
- [x] 统计二维 bigram 计数矩阵；
- [x] 加入平滑并归一化为条件概率；
- [x] 实现交叉熵 loss 和 perplexity；
- [x] 实现原始概率采样；
- [x] 实现 temperature 和 top-k 改进采样；
- [x] 保存和加载词表及推理概率矩阵；
- [x] 保留教学语料并增加真实公共领域语料对照；
- [x] 编写设计、学习、测试和运行文档。

### 验收清单

- [x] 概率矩阵每一行归一化为 1；
- [x] 验证集 loss 和测试集 perplexity 为有限值；
- [x] 采样结果长度符合请求；
- [x] 固定 seed 的改进采样结果可复现；
- [x] 保存、加载后的词表和概率矩阵一致；
- [x] 教学语料和真实语料均可完成训练、评估和生成；
- [x] 当前 3 个自动化测试通过；
- [x] 文档明确计数模型没有梯度、embedding 和长距离语义能力。

## 03：字符 trigram

阶段状态：

- [x] 阶段完成

### 实现清单

- [x] 使用前两个字符预测下一个字符；
- [x] 从真实语料构造 trigram 自监督样本；
- [x] 建立三维 trigram 计数和概率矩阵；
- [x] 只使用训练集统计计数；
- [x] 只使用验证集选择 smoothing；
- [x] 为未见 trigram 上下文实现 bigram backoff；
- [x] 保存和加载 trigram 概率、backoff 概率和上下文可见状态；
- [x] 使用 temperature 和 top-k 生成文本；
- [x] 与项目 02 的真实语料 bigram 结果独立对照；
- [x] 编写设计、学习、测试和运行文档。

### 验收清单

- [x] 三维概率矩阵沿下一字符维度归一化为 1；
- [x] 验证集 loss 和测试集 perplexity 为有限值；
- [x] 未见上下文可以回退到 bigram 分布；
- [x] 固定 seed 的生成结果可复现；
- [x] 保存、加载后的模型状态和生成结果一致；
- [x] 当前 3 个自动化测试通过；
- [x] 当前真实语料测试 perplexity 为 `17.423710`；
- [x] 文档明确短语料、上下文稀疏和生成质量边界。

## 04：显式字符 Tokenizer

阶段状态：

- [x] 阶段完成

### 目的

项目 02、03 已把字符作为 token，并使用 `to_id` 和 `ids` 完成隐式编码。本阶段把这部分逻辑提升为独立接口，为所有神经网络模型提供统一输入。

```text
文本 → Tokenizer.encode → token ID 序列
token ID 序列 → Tokenizer.decode → 文本
```

### 实现清单

- [x] `token_to_id` 和 `id_to_token` 双向映射；
- [x] `<BOS>`、`<EOS>`、`<UNK>` 等特殊 token；
- [x] 明确只使用训练数据建立词表的策略；
- [x] `encode()`、`decode()`、保存和加载；
- [x] 空文本、未知字符、特殊 token 和往返转换测试；
- [x] 使用显式 Tokenizer 重新表达 bigram 数据构造、训练和采样；
- [x] 保留项目 02、03 的历史代码和基线结果。

### 验收清单

- [x] `decode(encode(text))` 在支持范围内可以还原文本；
- [x] 未知字符行为明确且可测试；
- [x] BOS/EOS 是否加入由显式参数控制；
- [x] 保存、加载前后的词表和编码结果一致；
- [x] 阶段 04 的 bigram 样本构造只接收 token ID，不直接依赖字符串索引逻辑。

### 暂不包含

- BPE 或其他 subword 算法；
- embedding 和神经网络训练；
- 为提高生成质量而扩大模型。

## 05：Token Embedding 与 MLP

阶段状态：

- [ ] 阶段完成

### 目的

从统计计数概率表进入可通过梯度学习的神经网络参数：

```text
token ID
  → embedding
  → 固定窗口向量
  → 隐藏层与激活函数
  → logits
  → softmax
  → 下一个 token 概率
```

### 实现清单

- [ ] token embedding 查表；
- [ ] 固定长度上下文窗口；
- [ ] 线性层、激活函数、logits 和 softmax；
- [ ] 交叉熵与反向传播；
- [ ] 固定随机种子的训练和生成；
- [ ] 与项目 03 在相同语料切分上的 perplexity 对照。

### 验收清单

- [ ] 前向传播的张量形状正确；
- [ ] 可用数值梯度或小规模基准验证关键梯度；
- [ ] 训练 loss 在可控样本上下降；
- [ ] 保存、加载后相同输入产生相同输出；
- [ ] 报告能够解释 token ID、embedding 和 logits 的区别；
- [ ] 不把单次生成样例作为模型提升的主要证据。

## 06：训练工程基础

阶段状态：

- [ ] 阶段完成

### 目的

在模型继续变复杂前，形成可配置、可恢复、可复现的训练流程。

### 实现清单

- [ ] mini-batch 与数据迭代；
- [ ] SGD 和 Adam 对照；
- [ ] 学习率、batch size 和随机种子配置；
- [ ] 验证集 early stopping；
- [ ] checkpoint 保存与恢复训练；
- [ ] loss、梯度范数和训练耗时记录；
- [ ] 相同配置重复运行的一致性检查。

### 验收清单

- [ ] 中断后可以从 checkpoint 恢复训练；
- [ ] 恢复后的 step、参数和优化器状态连续；
- [ ] 训练状态、推理模型和实验报告彼此分离；
- [ ] 验证集用于选择，测试集只用于最终评估；
- [ ] Adam 的每个状态变量和更新步骤都可以解释。

## 07：字符级 Transformer

阶段状态：

- [ ] 阶段完成

### 目的

理解现代自回归语言模型的核心结构，而不是追求大模型级生成质量。

### 实现清单

- [ ] positional information；
- [ ] 单头 causal self-attention；
- [ ] causal mask；
- [ ] multi-head attention；
- [ ] residual connection；
- [ ] layer normalization；
- [ ] Transformer block；
- [ ] 多层堆叠与自回归生成。

### 验收清单

- [ ] attention、head、batch 和 sequence 维度有独立测试；
- [ ] causal mask 能证明当前位置不能读取未来 token；
- [ ] 单样本与 batch 输入行为一致；
- [ ] 小数据过拟合测试可以通过；
- [ ] 保存、加载后输出一致；
- [ ] 训练与验证 loss、perplexity 和生成样例分别解释。

### 暂不包含

- 分布式训练；
- GPU kernel 优化；
- RLHF 或 Agent 编排；
- 用小语料结果外推真实大模型能力。

## 08：Subword Tokenizer

阶段状态：

- [ ] 阶段完成

### 目的

在字符 token 已完全理解后，再学习 BPE 或同类子词方法：

```text
字符 token：词表小、序列长
subword token：词表更大、序列更短
```

### 实现与对照清单

- [ ] 从训练语料学习 merge 规则；
- [ ] encode/decode 与模型文件共同版本化；
- [ ] 比较字符和 subword 的词表大小；
- [ ] 比较同一文本的 token 数和平均序列长度；
- [ ] 检查未知文本、标点、中英文和代码片段；
- [ ] 解释真实大模型的 token 数为什么不等于字符数或单词数。

### 验收清单

- [ ] merge 训练不读取验证集和测试集；
- [ ] 相同词表和 merge 规则产生稳定编码；
- [ ] encode/decode 的可逆范围明确；
- [ ] 字符与 subword 对照使用相同语料和统计方法。

## 09：评估、推理与服务化

阶段状态：

- [ ] 阶段完成

### 目的

把模型训练和模型使用分开，建立最小但完整的模型生命周期。

### 实现清单

- [ ] 固定训练、验证和测试语料；
- [ ] 区分 perplexity 与人工生成质量；
- [ ] 模型配置、Tokenizer 和权重共同版本化；
- [ ] 批量推理与流式生成；
- [ ] 记录冷启动、单请求延迟、吞吐和内存；
- [ ] 提供最小推理接口；
- [ ] 记录不兼容模型和 Tokenizer 的明确错误。

### 验收清单

- [ ] 推理过程不依赖训练数据和优化器；
- [ ] 批量与单条推理语义一致；
- [ ] 流式输出与非流式最终结果的关系明确；
- [ ] 性能报告包含环境、输入规模、预热方式和重复次数；
- [ ] 性能结果只归属于当前小模型和当前环境，不外推为真实大模型或 GPU 推理性能。

## 阶段文档与证据规范

新增项目继续使用统一结构：

1. `README.md`：项目目标、运行方式和文件导航；
2. `design.md`：输入输出、核心逻辑、训练流程和边界；
3. `learning_notes.md`：学习顺序和概念导航；
4. `test_report.md`：自动化测试验证了什么、没有验证什么；
5. `run_report.md`：固定配置下的实际指标、样例和限制；
6. `tests/`：概率、形状、边界、保存加载和可复现性测试；
7. `artifacts/`：可重新生成的模型产物。

每个阶段都必须明确：

- 核心逻辑：模型到底计算了什么；
- 主要功能：模型解决什么问题；
- 输入输出：一次调用接收和返回什么；
- 训练方式：参数如何获得，目标从哪里来；
- 验证重点：用什么证据证明实现和结果可信；
- 能力边界：当前结果不能证明什么。

## 当前执行点

当前执行点是阶段 05。开始实现 Token MLP 前，先确认模型只接收阶段 04 产生的 token ID；Tokenizer 不负责 embedding、上下文窗口、loss 或梯度计算。
