# 10 运行报告（初始化）

当前已验证加载、教学模型的单条、顺序批量和流式生成、无重复目标计数的 perplexity 评估，以及加载冷启动、未预热首请求、预热后延迟和吞吐统计契约。`python3 -m pytest -q projects/stage10_inference_serving/tests/test_stage10.py` 得到 `29 passed`。当前尚未记录真实环境下的独立性能结果，也不构成服务化验收。

后续性能记录应列出模型文件及版本、设备与软件环境、输入与生成 token 长度、批量大小、冷启动定义、预热次数、重复次数、延迟分布、吞吐、峰值内存以及测量方法。评估记录应注明阶段 08 的固定数据切分和模型选择方式，并将 perplexity 与人工生成观察分开。
